<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_forecast"
            android:checked="true"
            android:icon="@drawable/forecast"
            android:title="Today's forecast" />
        <item
            android:id="@+id/nav_saved_locations"
            android:icon="@drawable/saved_location"
            android:title="Saved  locations" />
        <item
            android:id="@+id/nav_saved_forecasts"
            android:icon="@drawable/saved_forecast"
            android:title="Saved forecasts" />
    </group>

    <item android:title="Account">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/settings"
                android:title="Settings" />
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/logout"
                android:title="Log out" />
        </menu>
    </item>

</menu>
